import globalRoute from './routes/global';
import productRoute from './routes/products';
import ordersRoutes from './routes/orders-routes';


const routes = [globalRoute, productRoute, ordersRoutes];

export default routes;
